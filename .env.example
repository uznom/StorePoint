# StorePoint POS Environment Configuration
# StorePoint is a 100% offline-first POS system.
# No cloud AI services, API keys, or external application secrets are required for local builds or runtime.
